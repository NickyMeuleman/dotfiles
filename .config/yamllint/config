extends: default

rules:
  line-length: disable
  document-start: disable # allow "---" at start of a line
  truthy: disable # allow using values like `on` in github actions
  comments:
    min-spaces-from-content: 1
